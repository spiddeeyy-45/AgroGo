<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- Conditional: based on API level -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/a"
        android:label="@string/app_name"
        android:roundIcon="@drawable/a1"
        android:supportsRtl="true"
        android:theme="@style/Theme.AgroGo"
        tools:targetApi="31">
        <activity
            android:name="Farmer.FarmerDashBoard"
            android:exported="false" />
        <activity
            android:name="CommonUI.OnBoardingScreen"
            android:exported="false" />
        <activity
            android:name="Consumer.UserDashBoard"
            android:exported="false" />
        <activity
            android:name="CommonUI.SignUpScreen"
            android:exported="false" />
        <activity
            android:name="CommonUI.LoginScreen"
            android:exported="false" />
        <activity
            android:name="CommonUI.Splashscreen1"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>